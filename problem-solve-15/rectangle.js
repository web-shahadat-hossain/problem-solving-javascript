/* Write a function. Which will take the length and height of a rectangle as input parameters. Then the area of that rectangle will return as the result.
 */


function areaOfRectangle(length, height){
    let area=length*height;
    return area;
}

console.log( areaOfRectangle(40, 20));